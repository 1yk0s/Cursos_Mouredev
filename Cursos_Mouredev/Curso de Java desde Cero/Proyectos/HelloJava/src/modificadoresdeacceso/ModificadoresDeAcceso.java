package modificadoresdeacceso;

public class ModificadoresDeAcceso {

    //Encapsulamiento (Modificadores de acceso)

    // - Modificadores de acceso.
    // public (se puede acceder desde cualquier parte del programa).
    // private (solo se puede acceder desde dentro de la clase).
    // protected (acceder desde la misma clase y subclases).
    // default (solo se puede acceder en el mismo paquete).

    //Getters
    //Setters
}
